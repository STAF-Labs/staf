# Running STAF locally — Linux

> Not documented yet. The project is currently developed on Windows only.
>
> When Linux support is needed, mirror the flow in [windows.md](windows.md) with
> Linux equivalents (native `vendor/bin/sail`, `WWWUSER`/`WWWGROUP` from
> `id -u`/`id -g`, no Vite IPv6 workaround needed, etc.).
