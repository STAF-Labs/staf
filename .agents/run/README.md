# Running STAF locally

Canonical, crutch-free startup — backend via Laravel Sail (Docker), frontends via
Vite on the host. Pick your OS:

- **[windows.md](windows.md)** — Docker Desktop + PowerShell (current setup).
- **[linux.md](linux.md)** — placeholder; Windows-only for now.
